## Motivation

<!-- Why is this change needed? Link the issue if there is one. -->

## What changed

<!-- One behaviour per PR. List the concrete changes. -->

## Checklist

- [ ] `pip install -e .` then `pytest -q` passes
- [ ] `ruff check src tests` passes
- [ ] Rendered markdown is deterministic (golden tests untouched or updated)
- [ ] No real incident data in samples, fixtures, or tests
- [ ] No new dependencies, no network access
